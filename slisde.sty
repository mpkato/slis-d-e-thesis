\usepackage[a4paper]{geometry}

\makeatletter
\newcommand{\etitle}[1]{\def\@etitle{#1}}
\newcommand{\eauthor}[1]{\def\@eauthor{#1}}
\newcommand{\gradyear}[1]{\def\@gradyear{#1}}
\newcommand{\gradmonth}[1]{\def\@gradmonth{#1}}
\newcommand{\affiliation}[1]{\def\@affiliation{#1}}
\newcommand{\supervisor}[1]{\def\@supervisor{#1}}
\newcommand{\subsupervisor}[1]{\def\@subsupervisor{#1}}
\renewcommand{\maketitle}{
    \begin{titlingpage}
    %\begin{titlepage} % use this for the other documentclass
      \newgeometry{left=30truemm,right=30truemm,top=30truemm,bottom=30truemm}
      \let\footnotesize\small
      \let\footnoterule\relax
      \let\footnote\thanks
      \begin{center}
        \null\vfil
        {\fontsize{20truept}{28truept}\selectfont \textbf{\@title} \par}
        \vskip 26em
        {\fontsize{16truept}{24truept}\selectfont {\textbf \@gradmonth} {\textbf \@gradyear} \par}
        \vskip 3em
        {\fontsize{16truept}{24truept}\selectfont \textbf \@author \par}
        \vskip 3em
      \end{center}
    \end{titlingpage}
    \begin{titlingpage}
    %\begin{titlepage} % use this for the other documentclass
      \newgeometry{left=30truemm,right=30truemm,top=30truemm,bottom=30truemm}
      \let\footnotesize\small
      \let\footnoterule\relax
      \let\footnote\thanks
      \begin{center}
        \null\vfil
        {\fontsize{20truept}{28truept}\selectfont \textbf{\@title} \par}
        \vskip 20em
        {\fontsize{16truept}{24truept}\selectfont \textbf \@author \par}
        \vskip 6em
        {\fontsize{16truept}{24truept}\selectfont \textbf \@affiliation \par}
        \vskip 3em
        {\fontsize{16truept}{24truept}\selectfont {\textbf \@gradmonth} {\textbf \@gradyear} \par}
        \vskip 3em
      \end{center}
    \end{titlingpage}
  }%
  
\newenvironment{abst}
{% begin code
    \thispagestyle{empty}
    \begin{titlingpage}
    %\begin{titlepage} % use this for the other documentclass
      \newgeometry{left=30truemm,right=30truemm,top=35truemm,bottom=35truemm}
      \let\footnotesize\small
      \let\footnoterule\relax
      \let\footnote\thanks
      \begin{center}
        \fontsize{14truept}{20truept}\selectfont
        \textbf{\@title} \par
        \vskip 20truept
      \end{center}
      \fontsize{10.5truept}{16truept}\selectfont
      {\raggedleft Name: \@author \par}
        \vskip 20truept
      \par
}%
{
  \end{titlingpage}
}
